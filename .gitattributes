# Stored diffs preserve upstream whitespace in context lines.
*.patch whitespace=-trailing-space,-space-before-tab

# Keep copied license text byte-for-byte identical to its upstream source.
third-party/licenses/** whitespace=-trailing-space,-space-before-tab
